$(document).ready(() => {
    const form  = document.getElementById('form-post');
    form.addEventListener('submit', (e) => {
        event.preventDefault();
        $.ajax({
            
        });
    });
});